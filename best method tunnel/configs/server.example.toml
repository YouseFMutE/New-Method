role = "server"
transport = "app_tcp"
psk_hex = "REPLACE_WITH_64_HEX_CHARS"
max_frame_size = 1048576
reconnect_delay_ms = 1000
reconnect_max_delay_ms = 15000

[server]
tunnel_listen = "0.0.0.0:9000"
public_listen = "0.0.0.0:1414"
tunnel_allow_ips = ["203.0.113.10"]
